import Title from "@/components/Title";

const page = () => {
  return (
    <section className="fade-in flex flex-col gap-y-6">
      <Title title="Serie 2024" />
    </section>
  );
};

export default page;
